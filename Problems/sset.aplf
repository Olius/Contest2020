 sset←{             ⍝ ⍺⍺|⍺*⍵, avoiding overflow.
     ⍺←1000000 2    ⍝ For fun, this one-liner also does the job (albeit less readably):
     m a←⍺          ⍝ sset←{⊃{1e6|⍵×.*↑(1 0)(⍺ 2)}/(2⊥⍣¯1⊢⍵),⊂1 2}
     n←⍵
     modExp←{
         ⍺←1
         0=⍺⍺:⍺
         q r←0 2⊤⍺⍺
         (m|⍺×⍵*r)(q ∇∇)m|⍵*2
     }
     n modExp a
 }
